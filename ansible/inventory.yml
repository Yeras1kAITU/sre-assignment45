all:
  hosts:
    localhost:
      ansible_connection: local
      ansible_host: 127.0.0.1
  vars:
    ansible_python_interpreter: /usr/bin/python3
    docker_compose_dir: /mnt/c/Users/yeras/IdeaProjects/project
